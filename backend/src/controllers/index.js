const auth_controllers = require('./auth_controllers');
const home_controllers = require('./home_controllers');
const recharge_controllers = require('./recharge_controllers');

module.exports = {
    auth_controllers,
    home_controllers,
    recharge_controllers
};